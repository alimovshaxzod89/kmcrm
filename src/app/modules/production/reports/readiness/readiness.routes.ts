import {Route} from '@angular/router';
import {ReadinessComponent} from "./readiness.component";

export const readinessRoutes: Route[] = [
    {
        path: '',
        component: ReadinessComponent,
    }
];
