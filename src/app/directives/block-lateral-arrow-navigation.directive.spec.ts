import { BlockLateralArrowNavigationDirective } from './block-lateral-arrow-navigation.directive';

describe('BlockLateralArrowNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockLateralArrowNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
